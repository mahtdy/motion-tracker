# Project Instructions & Conventions

## Versioning Rule
- On every modification or release, always increment and synchronize the application version (e.g., in `package.json`, `index.html`, `app.js`, and `sw.js`).
- Never leave old version tags or stale cache names when introducing features or bug fixes.
